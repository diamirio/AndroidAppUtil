apply from: "$rootDir/library-module.gradle"

android {
    lintOptions {
        abortOnError false
    }
}

dependencies {
    implementation deps.rxjava2.core
    implementation deps.rxjava2.kotlin
    api deps.firebase.values()
}